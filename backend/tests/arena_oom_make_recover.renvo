package main

func arenaMakeTooLarge(n int) {
	data := make([]byte, n)
	data[n-1] = 1
}

func appMain() (status int) {
	status = 1
	defer func() {
		if recover() != nil {
			print("PASS\n")
			status = 0
		}
	}()
	arenaMakeTooLarge(4097)
	print("FAIL\n")
	return
}
